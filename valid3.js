const name = document.getElementsById('name')
const name = document.getElementById(email)
const name = document.getElementById('username')
const name = document.getElementById('password')
const name = document.getElementById('form')
const name = document.getElementById('error')

form.addEventListener('submit',() =>{
let messages = []
if (CheckPassword.value === '' || CheckPassword.value == /^(A-Z)/(a-z).(0-8));
messages.push('Please check password requirements')
}

if (messages.length > 0){
    e.preventDefault()
}

